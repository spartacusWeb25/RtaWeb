from django.apps import AppConfig


class ContribuintesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "contribuintes"
    verbose_name = "Contribuintes (Sócios / Diretores)"
